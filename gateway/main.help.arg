Narrowlink Gateway

Usage:
  narrowlink [options]

Options:
  -c, --config=    Specify a config file
  -h, --help       Print help information
      --version    Print version information

Environment:
  NARROWLINK_GATEWAY_CONFIG    Specify a config file when --config is not set

